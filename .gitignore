# PlatformIO build artifacts
firmware/.pio/
firmware/.vscode/

# OS
.DS_Store

# Claude Code
.claude/

# Node tooling deps (npm install --no-save)
tools/node_modules/
tools/package-lock.json

# v0.7.1 bundled Node binary (~120MB arm64). Downloaded on demand by
# tools/download-bundled-node.sh; gitignored to keep the repo lean.
# tools/build-mac-dmg.sh auto-runs the download before xcodebuild.
apple/ClawdmeterMac/Resources/Vendor/node/

# v0.29.12 bundled tmux + dylibs. Downloaded on demand by
# tools/download-bundled-tmux.sh; gitignored to keep the repo lean.
apple/ClawdmeterMac/Resources/Vendor/tmux/

# Apple — Xcode and xcodegen
apple/Clawdmeter.xcodeproj/xcuserdata/
apple/Clawdmeter.xcodeproj/project.xcworkspace/xcuserdata/
apple/**/.build/
apple/**/DerivedData/
apple/**/*.xcuserstate

# Mac DMG build artifacts (tools/build-mac-dmg.sh)
dist/
.build/

# v0.23.0 bundled opencode binary (~50MB arm64). Downloaded on demand by
# tools/download-bundled-opencode.sh; gitignored to keep the repo lean.
apple/ClawdmeterMac/Resources/Vendor/opencode/opencode
